# TRMNL plugin UUID — find it in the TRMNL dashboard URL: /plugin_settings/{UUID}
TRMNL_PLUGIN_UUID=your-plugin-uuid-here

# Optional: override today's date for testing (YYYY-MM-DD)
# OVERRIDE_DATE=2026-02-25
